import React from 'react';
import UseTitle from '../../hooks/UstTitle';

const Services = () => {
    UseTitle('Services')
    return (
        <div>
            <h2>Services</h2>
        </div>
    );
};

export default Services;