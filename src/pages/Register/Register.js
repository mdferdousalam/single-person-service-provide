import React from 'react';
import UseTitle from '../../hooks/UstTitle';

const Register = () => {
    UseTitle('Register')
    return (
        <div>
            <h2>Register</h2>
        </div>
    );
};

export default Register;