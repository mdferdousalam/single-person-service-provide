import React from 'react';
import UseTitle from '../../hooks/UstTitle';

const Login = () => {
    UseTitle('Login')
    return (
        <div>
            <h2>Login</h2>
        </div>
    );
};

export default Login;