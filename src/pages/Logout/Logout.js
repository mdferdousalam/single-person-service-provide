import React from 'react';
import UseTitle from '../../hooks/UstTitle';

const Logout = () => {
    UseTitle('Log Out')
    return (
        <div>
            <h2>Logout</h2>
        </div>
    );
};

export default Logout;